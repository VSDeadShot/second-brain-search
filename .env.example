# Copy to .env and fill in. .env is gitignored - never commit real values.

# Absolute path to the parent folder holding all your project repos.
SBS_SCAN_ROOT=C:/Users/vedan/Desktop/VSDeadShot/Projects

# Gemini API key - used from slice 2 onward (embeddings + RAG answers).
# Not required for `sbs discover`.
GEMINI_API_KEY=
